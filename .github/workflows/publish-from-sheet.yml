name: Publish Recipes From URL Inbox

on:
  workflow_dispatch:
    inputs:
      recheck_existing:
        description: "Recheck existing URLs and republish only when changed"
        required: false
        type: boolean
        default: false
      limit:
        description: "Max URLs to process this run"
        required: false
        type: number
        default: 25
  schedule:
    - cron: "*/5 * * * *"

permissions:
  contents: write

concurrency:
  group: publish-recipes
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: Pull URL Inbox
        env:
          URL_INBOX_CSV_URL: ${{ secrets.URL_INBOX_CSV_URL }}
        run: node scripts/fetch-sheet-urls.js --out content/source-urls.txt

      - name: Start Local Backend
        env:
          ADMIN_KEY: ${{ secrets.ADMIN_KEY }}
          SITE_URL: ${{ vars.SITE_URL }}
        run: |
          node server.js > /tmp/miseflow-server.log 2>&1 &
          echo $! > /tmp/miseflow-server.pid
          sleep 3

      - name: Publish URLs
        env:
          ADMIN_KEY: ${{ secrets.ADMIN_KEY }}
          RUN_LIMIT: ${{ github.event_name == 'workflow_dispatch' && inputs.limit || 25 }}
          RECHECK_EXISTING: ${{ github.event_name == 'workflow_dispatch' && inputs.recheck_existing || false }}
        run: |
          ARGS="--file content/source-urls.txt --api http://localhost:3030 --limit ${RUN_LIMIT}"
          if [ "${RECHECK_EXISTING}" = "true" ]; then
            ARGS="${ARGS} --recheck-existing"
          fi
          npm run publish:urls -- ${ARGS}

      - name: Stop Local Backend
        if: always()
        run: |
          if [ -f /tmp/miseflow-server.pid ]; then
            kill "$(cat /tmp/miseflow-server.pid)" || true
          fi

      - name: Rebuild Static Files
        run: npm run build:static

      - name: Commit And Push If Changed
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add content/recipes content/source-urls.txt recipes sitemap.xml
            git commit -m "chore: auto-publish recipes from URL inbox"
            git push
          else
            echo "No changes to commit."
          fi
